services:
  web:
    build: .
    ports:
      - "8080:80"
    env_file:
      - .env
    volumes:
      - .:/var/www/html
      - images_data:/var/www/html/images
    restart: unless-stopped

volumes:
  images_data:
